const DRAWER_WIDTH = 240;
const enum MODAL_TYPES {
  LOGIN = 'LOGIN',
}

const CONSTANTS = {
  VISUAL: {
    DRAWER_WIDTH,
  },
  MODAL: {
    TYPES: {
      LOGIN: MODAL_TYPES.LOGIN,
    },
  },
};

export default CONSTANTS;
